[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pcr"
version = "0.1.0"
description = "Probabilistic Coastline Recession (PCR) model"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["pcr"]
